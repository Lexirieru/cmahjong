[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.30"
evm_version = "cancun"       # Celo L2 (OP Stack) supports Cancun; OZ v5 needs the mcopy opcode
optimizer = true
optimizer_runs = 200

# RPC endpoint read from .env (CELO_RPC). For testing switch to Alfajores.
[rpc_endpoints]
celo = "${CELO_RPC}"
alfajores = "https://alfajores-forno.celo-testnet.org"

# Contract verification via Celoscan (Etherscan v2 multichain API).
[etherscan]
celo = { key = "${ETHERSCAN_API_KEY}", chain = 42220 }
alfajores = { key = "${ETHERSCAN_API_KEY}", chain = 44787 }

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options
